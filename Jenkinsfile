pipeline {
    agent {
        dockerfile {
            dir 'UAT'
        }
    }

    stages {
    
        stage('build gns3') {
            steps {
                sh 'ls -al /'
            }
            
        }
    }
}