pipeline {
    agent {
        docker {
            image 'node:7-alpine'
        }
    }

    stages {
    
        stage('build gns3') {
            steps {
                sh ' node --version'
            }
            
        }
    }
}