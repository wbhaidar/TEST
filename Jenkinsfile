pipeline {
    agent {
        dockerfile {
            dir 'UAT'
            label 'taf'
        }
    }

    stages {
    
        stage('build gns3') {
            steps {
                sh 'ls -al /'
            }
            
        }
    }
}